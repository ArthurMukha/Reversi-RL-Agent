# Reversi RL Agent

[![CI](https://github.com/ArthurMukha/Reversi-RL-Agent/actions/workflows/ci.yml/badge.svg)](https://github.com/ArthurMukha/Reversi-RL-Agent/actions/workflows/ci.yml)

Игра **Реверси (Отелло)** в браузере на Go + нейросетевой агент на PyTorch,
обученный методами обучения с подкреплением.

Цель проекта — не просто играбельная версия Реверси, а обученный агент
приличного уровня, с которым можно сразиться прямо в браузере.

## Архитектура

Два сервиса, разделённые по ответственности:

```
браузер  ──HTTP/JSON──▶  game-service (Go)  ──gRPC──▶  model-service (Python)
                          правила + веб                инференс модели
```

- **Правила игры живут только в Go.** Модель их не знает: получает позицию
  и список легальных ходов, возвращает выбранный. Go проверяет ответ и не
  доверяет ему на слово.
- **Запрос к модели не имеет состояния** — каждый вызов несёт полную позицию,
  поэтому сервис модели можно перезапустить посреди партии.
- Браузер не умеет в gRPC напрямую, поэтому браузер ↔ Go остаётся HTTP/JSON.
- Оба сервиса слушают только `127.0.0.1` — это dev-окружение, не хостинг.

## Стек

- **Go** — движок, HTTP API, веб-интерфейс. Только stdlib: `net/http`,
  `embed`, `encoding/json`, `testing`. Плюс `google.golang.org/grpc` для
  связи с моделью.
- **Python 3 + PyTorch** — обучение и инференс: свёрточная policy-value сеть
  с residual-блоками.
- **Protocol Buffers / gRPC** — контракт между сервисами.
- **Ванильный HTML/CSS/JS** — фронтенд без фреймворков.

## Структура

```
reversi-rl-agent/
├── proto/model_service/v1/    # контракт gRPC между Go и Python
├── game-service/              # Go
│   ├── cmd/server/            # HTTP-сервер (127.0.0.1:8080) + тесты API
│   ├── internal/game/         # чистый движок Реверси + тесты
│   ├── internal/aiclient/     # gRPC-клиент к model-service
│   ├── internal/pb/           # сгенерированный код контракта
│   ├── web/                   # интерфейс, встроен через //go:embed
│   └── Dockerfile             # multi-stage: статический бинарник на distroless
├── model_service/             # Python: gRPC-сервер, только инференс
│   ├── net.py                 # архитектура сети
│   ├── checkpoint.py          # загрузка весов
│   ├── server.py              # gRPC-сервер
│   ├── checkpoints/           # отобранные веса (едут в git)
│   └── Dockerfile             # образ с CPU-сборкой torch
├── training/training.ipynb    # обучение: self-play, MCTS, арена
├── .github/workflows/ci.yml   # проверки на каждый push и pull request
├── compose.yaml               # оба сервиса одной командой
├── pyproject.toml             # настройки ruff
└── Makefile                   # проверки, запуск, генерация кода из .proto
```

## Запуск

Нужен только Docker:

```bash
docker compose up --build     # http://127.0.0.1:8080
docker compose down           # остановить
```

Наружу смотрит один порт — 8080 на `127.0.0.1`. Сервис модели не
публикуется: игровой сервер находит его по имени во внутренней сети
Compose.

### Без Docker

Оба сервиса запускаются и вручную — так удобнее во время разработки.
Игра человек-против-человека работает без Python: без сервиса модели
запрос хода ИИ возвращает 503, а партия продолжается.

```bash
# веб-интерфейс: http://127.0.0.1:8080
go -C game-service run ./cmd/server/

# сервис модели (в отдельном терминале, из корня репозитория)
model_service/.venv/bin/python -m model_service.server
```

Адреса и идентификатор модели читаются из переменных окружения
(`LISTEN_ADDR`, `MODEL_SERVICE_ADDR`, `MODEL_ID`); их значения по
умолчанию совпадают с командами выше, поэтому задавать ничего не нужно.

## Разработка

```bash
make check      # ruff + go build, vet, gofmt, test -race — всё, что гоняет CI
make up         # оба сервиса в фоне, печатает адрес
make dev        # check, и только если прошёл — up
make down       # остановить
make proto      # перегенерировать код из .proto
```

`make check` — то же самое, что выполняет GitHub Actions на каждый push и
pull request, поэтому красный CI после зелёного `check` означает разницу
между машинами, а не между проверками.

Тестами покрыты обе стороны Go-сервиса. `internal/game` проверяет
правила: отказ нелегального хода, перевороты сразу в нескольких
направлениях, пропуск хода, конец партии. `cmd/server` проверяет
HTTP-слой через `httptest` — маршрутизацию (неверный метод даёт 405,
неизвестный путь 404) и ответы всех четырёх эндпоинтов, включая ветки,
где сервис модели недоступен, вернул мусор или предложил нелегальный ход.
Клиент модели подменяется заглушкой, поэтому прогон не требует ни Python,
ни сети и занимает около секунды.

Python-окружение для локального запуска и генерации кода:

```bash
python -m venv model_service/.venv
model_service/.venv/bin/pip install -r model_service/requirements-dev.txt
```

`requirements.txt` — только то, что нужно в рантайме (оно и едет в образ);
`requirements-dev.txt` добавляет к нему `grpcio-tools` для `make proto` и
`ruff` для проверок. CPU-сборка torch подтягивается автоматически: индекс
прописан в самом файле зависимостей.

Линтер и форматтер — `ruff`, настройки в `pyproject.toml`. В CI ставится
он один, без остальных зависимостей: `make check RUFF=ruff` берёт
исполняемый файл из `PATH` вместо виртуального окружения, и раннеру не
приходится качать torch ради проверки стиля.

Сгенерированный из `.proto` код коммитится в репозиторий, чтобы проект
собирался без `protoc` и плагинов.

## Как обучалась модель

Ноутбук `training/training.ipynb` реализует цикл в духе AlphaZero: сеть играет
сама с собой, партии складываются в replay buffer, на них дообучается новая
версия, после чего кандидат играет матч против текущей лучшей в «арене» и
принимается только при уверенном перевесе. Отобранные веса лежат в
`model_service/checkpoints/`.

Поиск батчевый: деревья всех партий продвигаются синхронно, и листья
оцениваются одним проходом сети вместо прохода на партию. Доска внутри —
две 64-битные маски, ходы генерируются сдвигами без единого цикла по
клеткам. Правила при этом не менялись, и это проверено: перед заменой
реализации обе играли одни и те же случайные партии со сверкой позиции,
очереди хода, легальных ходов обоих цветов и результата на каждом ходу.

## Roadmap

- [x] 1. Пофиксить баги в обучении модели.
- [x] 1.5. Оптимизировать процесс обучения.
- [x] 2. Обучить более-менее качественную модель для игры.
- [x] 3. Сделать игровой интерфейс.
- [x] 4. Подключить обученную модель к игровому интерфейсу.
- [ ] 5. Обучить различные архитектуры моделей и добавить в игру
      возможность выбрать, против какой из них играть.

### Дальше

**Запуск и эксплуатация**
- [x] 6. Docker Compose: оба сервиса одной командой, адреса и id модели — из
      переменных окружения.
- [x] 7. CI на GitHub Actions: `ruff`, `go build`, `go vet`, `gofmt`,
      `go test -race` — одной целью `make check`. Сборка образов пока не в
      CI: она дублирует то, что и так проверяется, а времени берёт больше
      всего остального вместе.
- [x] 8. Тесты HTTP-слоя на `httptest` с заглушкой вместо клиента модели:
      маршрутизация, ответы всех четырёх эндпоинтов и ветки ошибок
      ИИ-хода. Python-часть тестами пока не покрыта.
- [ ] 9. Метрики Prometheus: время инференса, ходы в секунду, ошибки по кодам.
- [ ] 10. gRPC health checking и server reflection.

**Сила игры**
- [ ] 11. MCTS в сервисе инференса — после теста на совпадение правил Python
      и Go.
- [ ] 12. Арена между чекпоинтами, ELO и таблица версий в интерфейсе.
- [ ] 13. Уровни сложности через температуру — вывести ручку в интерфейс.
- [ ] 14. Подсказка «лучший ход» для человека.

**Производительность**
- [ ] 15. TorchScript или ONNX Runtime, замер до и после.
- [ ] 16. Батчинг запросов на стороне сервиса модели.
- [ ] 17. Нагрузочное тестирование gRPC, фиксация p50/p99.

**Анализ партии**
- [ ] 18. Реплей: история ходов, перемотка, ссылка на партию.
- [ ] 19. График `value` по ходам — видно, где партия переломилась.
- [ ] 20. LLM-комментатор: локальная модель получает позицию, ход и оценку
      `value` и пишет комментарий по ходу партии; отдельный сервис со своим
      контрактом.
